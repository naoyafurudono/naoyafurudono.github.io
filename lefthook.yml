pre-push:
  piped: true
  commands:
    check:
      run: (cd tools/tt; npm run check)
      priority: 1
    deploy:
      root: tools/tt
      run: (cd tools/tt; npm run deploy:all)
      priority: 2
